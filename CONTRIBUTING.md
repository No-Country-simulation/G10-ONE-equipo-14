# Contributing

1. Branch from `main`.
2. Use focused branches: `feature/...`, `fix/...`, `docs/...`.
3. Keep public API/schema changes explicit.
4. Run `pytest -q` before opening a PR.
5. Never commit `.env`, API keys or OCI credentials.
6. Include tests for contract or domain changes.
